import { BannerAdResponse } from "../../modules/AdFormats/Banner/types";
import { AdServer } from "./AdServer";
type BannerAdParams = {
    placementId: string;
    walletAddress?: string;
    userEmail?: string;
    requestId: string;
    detectedWalletAddress?: string;
};
export declare class AdServerController {
    private _adServer;
    private _deviceMetadata;
    constructor(adServer: AdServer);
    private _getDeviceInfo;
    fetchBannerAd(params: BannerAdParams): Promise<{
        data: BannerAdResponse;
        status: number;
    } | undefined>;
}
export {};
