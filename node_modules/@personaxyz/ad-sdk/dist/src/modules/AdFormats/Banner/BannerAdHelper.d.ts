import { ENVIRONMENT } from "../../../types";
export declare class BannerAdHelper {
    protected _getFallbackAd(dimensions: {
        width: string;
        height: string;
    }): {
        mediaUrl: string;
        ctaUrl: string;
    };
    protected _generateCreative(creativePlaceholderDiv: HTMLElement, dimensions: {
        width: string;
        height: string;
    }, mediaUrl: string, environment: ENVIRONMENT, creativeId: string, placementId: string, ctaUrl?: string, requestId?: string): HTMLImageElement;
    protected _generateFallbackCreative(creativePlaceholderDiv: HTMLElement, dimensions: {
        width: string;
        height: string;
    }): HTMLImageElement;
    protected _getWatermarkElement(): HTMLAnchorElement;
    protected _isCreativePresent(adSlot: HTMLElement): boolean;
    protected _getCreativeComputedWidth(adSlot: HTMLElement): number;
}
