import { AllowedTopLevelDomains, NameService, Network, RpcURLs } from './name-service';
export declare const serviceID = "icns";
export declare class ICNS extends NameService {
    serviceID: string;
    chain: string;
    contractAddress: {
        mainnet: string;
        testnet: string;
    };
    resolve(name: string, network: Network, options?: {
        allowedTopLevelDomains?: AllowedTopLevelDomains;
        rpcUrls?: RpcURLs;
    }): Promise<string>;
    lookup(address: string, network: Network, options?: {
        rpcUrls?: RpcURLs;
    }): Promise<string>;
}
